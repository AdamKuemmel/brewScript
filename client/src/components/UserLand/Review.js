import React from "react";
import { useLocation, useHistory } from "react-router-dom";

const Review = () => {
  return (
    <div>
      heres a review of yourders and past ordersa nd your next order and order
    </div>
  );
};

export default Review;
