import React from "react";
import { useQuery } from "@apollo/client";
import SubOptions from "../components/Pricing/SubOptions";

const Pricing = () => {
  return (
    <main>
      <SubOptions />
    </main>
  );
};

export default Pricing;
